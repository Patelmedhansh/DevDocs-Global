name: Auto-Translate Documentation

on:
  push:
    branches: [ main ]
    paths:
      - 'docs/**'
      - 'package.json'
      - '.github/workflows/**'
  pull_request:
    branches: [ main ]

permissions:
  contents: write
  pull-requests: write

jobs:
  translate:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Clean old translations
      run: |
        echo "Removing old translation folders..."
        rm -rf docs/es docs/fr docs/de docs/ja docs/hi docs/zh
        echo "✓ Old translations cleaned"

    - name: Run translations
      env:
        LINGODOTDEV_API_KEY: \${{ secrets.LINGODOTDEV_API_KEY }}
      run: |
        echo "Starting translation process..."
        npm run translate
        echo "✓ Translation complete"

    - name: Configure git
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"

    - name: Add changes
      run: git add .lingo-cache/docs_en_getting_started_md.json

    - name: Commit changes
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git commit -m "Update docs_en_getting_started_md.json" || echo "No changes to commit"

    - name: Commit and push translations
      env:
        GITHUB_TOKEN: \${{ secrets.GITHUB_TOKEN }}
      run: |
        if git diff --quiet; then
          echo "✓ No changes to commit"
        else
          echo "Committing translations..."
          git add docs/
          git commit -m "🌍 chore: Auto-translate documentation"
          git push origin main
          echo "✓ Translations pushed successfully"
        fi
